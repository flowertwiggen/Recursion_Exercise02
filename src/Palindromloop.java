/*
public class Palindrom {

    String r = "";

    public boolean checkTexten(String s1) {

        String s2 = s1.replaceAll("\\s","").toLowerCase();

        for(int i = s1.length()-1; i >= 0; i--) {
                r = r + s1.charAt(i);
        }
        return (r.equals(s2));
    }

}
*/